// @flow
import React, { Component } from 'react';
import { View } from 'react-native';

export type ClippedTransitionProps = {
  style?: View.propTypes.style,
  children: any,
  debug?: boolean,
};

type StateType = {};

export class ClippedTransition extends Component<ClippedTransitionProps> {
  render() {}
}
